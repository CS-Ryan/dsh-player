# dsh-replay 产品文档

| 项目     | 内容                                                         |
| -------- | ------------------------------------------------------------ |
| 产品名称 | dsh-replay                                                   |
| 文档版本 | v2.0                                                         |
| 更新日期 | 2026-09-13                                                   |
| 产品定位 | DSH 会话日志的可视化回放器                                   |
| 一句话   | 把不可读的 `session.jsonl.zstd` 变成可播放、可定位、可分享的会话时间线 |
| 设计原则 | 小而美、只读、本地、零依赖、只解决一件事                     |

---

## 1. 产品定位

`dsh-replay` 不是工具集，不是审计平台，不是成本分析器。

它是一个**小而美的 DSH Web Client 插件**，只做一件事：

> **让 Agent 会话日志变得可读、可回放、可定位。**

它把 DSH 底层 append-only 的 `session.jsonl.zstd` 日志，还原成一条人类可读的交互式时间线。用户像看视频一样回看 Agent 到底做了什么、在哪一步出错、哪一步调用了什么工具。

### 1.1 为什么只做一件事

DSH 已经完整记录了会话，但日志是底层事件流，人眼不可读。这是当前最痛的缺口。

成本估算、安全审计、血缘树、会话对比、团队共享——这些都有价值，但它们是“工具集”思维。一旦全做，插件会变重、变复杂、维护成本飙升，偏离“小而美”。

`dsh-replay` 选择聚焦：**先把“不可读”变成“可读”**。其他能力留给未来的独立插件或生态。

---

## 2. 痛点与场景

### 2.1 当前痛点

- `session.jsonl.zstd` 是压缩的底层事件流，无法直接阅读。
- Agent 跑完后，用户不知道它做了什么、哪一步失败、哪一步调用了危险工具。
- 想复盘一次失败任务，只能在原始日志里翻找。
- 想分享一次会话，只能发原始日志，对方无法阅读。
- DSH 缺少一个直观的会话回放界面。

### 2.2 典型场景

| 场景            | 用户动作                       | 价值         |
| --------------- | ------------------------------ | ------------ |
| 调试失败任务    | 打开会话，跳转到错误步骤       | 快速定位问题 |
| 理解 Agent 行为 | 播放时间线，查看推理与工具调用 | 建立信任     |
| 分享案例        | 导出独立 HTML 发给同事         | 无需安装 DSH |
| 查找特定操作    | 搜索工具名或关键词             | 秒级定位     |
| 复盘长会话      | 按 Turn/Step 折叠浏览          | 降低认知负担 |

### 2.3 用户故事

- 作为 Agent 开发者，我希望逐帧回放会话，快速定位失败步骤。
- 作为 DSH 用户，我希望点一下就能看懂 Agent 刚才做了什么。
- 作为支持人员，我希望导出一个 HTML，让对方无需安装 DSH 就能查看。
- 作为研究者，我希望搜索某个工具的所有调用记录。

---

## 3. 功能范围

### 3.1 核心功能（P0）

只做四件事：

1. **打开与解析**
2. **时间线回放**
3. **详情查看**
4. **搜索与导出**

### 3.2 明确不做（非目标）

| 不做       | 原因                           |
| ---------- | ------------------------------ |
| 成本估算   | 属于分析工具，增加定价维护负担 |
| 安全审计   | 属于审计平台，规则引擎过重     |
| 血缘树     | 属于会话管理，复杂度高         |
| 会话对比   | 属于高级分析，偏离核心         |
| 标记与注释 | 增加数据存储与同步复杂度       |
| 实时跟随   | 需要与运行时深度集成           |
| 团队共享   | 需要后端与权限体系             |
| 指标导出   | 属于可观测性平台               |

> 这些能力未来可以由独立插件实现。`dsh-replay` 只负责“让日志可读”。

---

## 4. 功能需求

### 4.1 打开与解析

- 从 DSH 会话列表一键打开。
- 拖拽 `.jsonl.zstd` 文件到界面。
- CLI：`dsh replay <session.jsonl.zstd>`。
- 支持多帧 Zstandard 解码。
- 还原事件类型：
  - 用户消息
  - 助手消息
  - 推理块
  - 工具调用
  - 工具结果
  - 审批请求与结果
  - 错误
  - 压缩/摘要
  - fork / 子 Agent
- 将扁平事件重构为 `Session → Turn → Step → Event`。
- 未知事件类型降级显示为原始事件，不阻塞解析。
- 解析失败时给出明确错误与原始日志下载入口。

### 4.2 时间线回放

界面布局：

```text
┌──────────────────────────────────────────────────────────────┐
│ 顶部：播放控制 | 搜索 | 导出                                  │
├──────────────┬──────────────────────────────┬────────────────┤
│ 左侧         │ 中部                         │ 右侧           │
│ 会话树       │ 时间线                       │ 详情检查器     │
│ Turn / Step  │ 事件卡片                     │ 内容 / JSON    │
│              │ 用户、助手、推理、工具、错误 │ Token / 耗时   │
└──────────────┴──────────────────────────────┴────────────────┘
```

- 播放、暂停、上一帧、下一帧。
- 支持 1x、2x、4x、8x 变速。
- 支持拖动进度条跳转。
- 支持按 `seq` 精确跳转。
- 键盘快捷键：
  - 空格：播放/暂停
  - `←` / `→`：上一步/下一步
  - `J` / `K`：上一个/下一个工具调用
  - `/`：搜索
- 时间线节点颜色区分事件类型。
- 长会话按 Turn/Step 折叠，避免一次性渲染全部节点。

### 4.3 详情查看

右侧详情面板展示当前选中事件的完整信息：

- 格式化内容与原始 JSON 切换。
- 模型、Token 计数、耗时。
- 工具名、参数、返回结果、退出码。
- 推理内容可折叠。
- 错误堆栈可展开。
- 支持复制字段路径。
- 支持跳转到相关 `seq` 或父事件。

### 4.4 搜索与过滤

- 全文搜索消息内容、工具参数、工具结果。
- 支持正则表达式。
- 按事件类型过滤：仅看工具调用、仅看错误、仅看推理。
- 按工具名过滤：如 `shell.exec`、`file.read`。
- 搜索结果直接定位到时间线位置。

### 4.5 导出与分享

- 一键导出独立 HTML 文件。
- 内嵌完整交互式回放界面与数据。
- 接收方无需安装 DSH 或插件。
- 支持脱敏选项：
  - API Key
  - 环境变量
  - 文件路径
  - 用户自定义正则
- 导出前提供预览。

---

## 5. 交互流程

1. 用户打开 DSH 会话或拖入 `.jsonl.zstd`。
2. 插件解析日志并构建时间线。
3. 用户播放、步进或跳转到目标事件。
4. 右侧查看详情，搜索框定位关键词。
5. 用户导出 HTML 分享。

---

## 6. 技术方案

### 6.1 插件形态

- DSH Web Client 插件，遵循 Cordis 插件体系。
- 挂载点：
  - 会话详情页的“回放”标签
  - 侧边栏入口
  - 命令面板
  - CLI：`dsh replay`
- 安装：`dsh plugin add dsh-replay`

### 6.2 解析器

- 独立实现 `session.jsonl.zstd` 解析。
- 支持多帧 Zstandard 解码。
- 优先使用 Node.js 内置 zstd，提供 WASM 回退。
- 解析器与 UI 解耦，便于单独测试。

### 6.3 性能

- 大文件解析放入 Web Worker，避免阻塞 UI。
- 时间线使用虚拟滚动，只渲染可视区域。
- 解析结果缓存到 IndexedDB。
- 100MB 日志首屏 ≤ 3s，回放操作响应 ≤ 100ms。

### 6.4 数据模型

```text
Session
  ├── Turn
  │     ├── Step
  │     │     ├── Event
  │     │     └── Event
  │     └── Step
  └── Fork
```

事件核心字段：

| 字段      | 说明                    |
| --------- | ----------------------- |
| seq       | 序列号                  |
| ts        | 时间戳                  |
| type      | 事件类型                |
| role      | user / assistant / tool |
| model     | 模型名                  |
| content   | 内容                    |
| toolName  | 工具名                  |
| args      | 工具参数                |
| result    | 工具结果                |
| tokens    | Token 统计              |
| duration  | 耗时                    |
| parentSeq | 父事件                  |
| forkId    | fork 标识               |

---

## 7. 非功能需求

| 类别     | 要求                             |
| -------- | -------------------------------- |
| 轻量     | 安装包尽量小，零外部依赖优先     |
| 只读     | 不修改、不重放执行、不调用工具   |
| 本地     | 默认本地处理，不上传会话内容     |
| 性能     | 100MB 日志首屏 ≤ 3s              |
| 内存     | 虚拟滚动，内存占用 ≤ 500MB       |
| 兼容     | 支持日志格式 v1/v2，未知事件降级 |
| 可访问性 | 键盘操作、ARIA、WCAG AA          |
| 国际化   | 首发中英文                       |
| 稳定性   | 解析失败不崩溃，提供错误定位     |

---

## 8. 成功指标

| 指标                  | 目标    |
| --------------------- | ------- |
| 解析成功率            | ≥ 99.5% |
| 100MB 日志首屏        | ≤ 3s    |
| 回放操作响应          | ≤ 100ms |
| 导出 HTML 离线可用率  | 100%    |
| 安装后 7 日活跃使用率 | ≥ 40%   |
| 用户调试时间减少      | ≥ 30%   |

---

## 9. 路线图

| 阶段 | 版本 | 内容                   |
| ---- | ---- | ---------------------- |
| M0   | v0.1 | 解析器原型、时间线展示 |
| M1   | v0.3 | 回放控制、详情面板     |
| M2   | v0.5 | 搜索、过滤             |
| M3   | v1.0 | 导出 HTML、稳定发布    |
| M4   | v1.1 | 性能优化、国际化       |
| M5   | v1.2 | 可选：实时跟随         |

---

## 10. 风险与依赖

| 风险          | 影响             | 缓解                         |
| ------------- | ---------------- | ---------------------------- |
| 日志格式变化  | 解析失败         | 版本化解析器，未知事件降级   |
| zstd 兼容问题 | 无法打开         | 优先内置支持，提供 WASM 回退 |
| 大文件性能差  | 卡顿             | Worker + 虚拟滚动 + 增量解析 |
| 隐私泄露      | 导出泄露敏感信息 | 默认脱敏，导出前预览         |
| 范围蔓延      | 插件变重         | 明确非目标，拒绝工具集化     |

---

## 11. 验收标准

- 给定一个含 10 个 Turn 的 `.jsonl.zstd`，能正确分组并展示时间线。
- 给定含工具调用的会话，能正确展示参数与结果。
- 给定含错误的会话，能定位到错误步骤。
- 导出 HTML 后，在无 DSH 环境用浏览器打开，可完整回放。
- 搜索、过滤结果与原始日志一致。
- 解析失败时给出明确错误信息，不丢失原始文件。
- 插件安装后不影响 DSH 原有功能与性能。

---

## 12. 开放问题

- 是否需要支持运行中会话的实时跟随？
- 导出 HTML 是否需要密码保护？
- 是否需要支持多会话标签页？
- 是否需要提供解析器独立包，供其他插件复用？

---

## 附录：术语表

| 术语               | 说明                                |
| ------------------ | ----------------------------------- |
| DSH                | DeepSeek Harness                    |
| Replay             | 会话回放                            |
| Turn               | 一次用户输入到 Agent 完成响应的周期 |
| Step               | Turn 内的一个执行步骤               |
| Fork               | 会话分叉                            |
| seq                | 日志事件序列号                      |
| session.jsonl.zstd | DSH 会话日志格式                    |

---

**总结**：`dsh-replay` 是一个只做“会话日志可视化回放”的小而美插件。它不追求功能大而全，只追求把 DSH 当前最痛的“日志不可读”问题解决好。安装轻、使用简单、只读安全、可离线分享。